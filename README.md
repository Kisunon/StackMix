# StackMix
StackMix is a deep-learning model for predicting single-cell transcriptional responses to unseen genetic perturbations. It combines the pretrained Stack tabular-transformer foundation model as a context-aware cell encoder with a Gene Ontology (GO) graph neural network that produces generalizable perturbation embeddings.
